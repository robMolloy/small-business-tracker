import React from "react";
import MUIMailIcon from "@material-ui/icons/Mail";
import Icon from "./Icon";

const NewIcon = (props) => <Icon Component={MUIMailIcon} {...props} />;
export default NewIcon;
