import React from "react";
import MUIBackSpaceIcon from "@material-ui/icons/BackSpace";
import Icon from "./Icon";

const NewIcon = (props) => <Icon Component={MUIBackSpaceIcon} {...props} />;
export default NewIcon;
